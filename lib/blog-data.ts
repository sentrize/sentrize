// Generated from content-export by the migration generator. Do not edit by hand.
export interface Post {
  slug: string; title: string; category: string; date: string; author: string;
  readTime: string; seoTitle: string; seoDescription: string; html: string;
}

export const POSTS: Post[] = [
  {
    "slug": "cutting-cloud-spend-by-40-percent",
    "title": "Cutting Cloud Spend by 40% Without Touching Performance",
    "category": "Cloud",
    "date": "Jan 2026",
    "author": "Priya Nair",
    "readTime": "7 min read",
    "seoTitle": "Cutting Cloud Spend by 40% Without Touching Performance",
    "seoDescription": "The FinOps audit framework we run for clients: right-sizing, idle capacity, storage tiers, and commitments — in the order that actually works.",
    "html": "<p>Most cloud bills are inflated by idle capacity and mis-sized instances, not traffic. When a CFO asks why the AWS invoice doubled, the honest answer is rarely \"because the business grew.\" It's usually a mix of instances sized by guesswork, non-production environments running around the clock, storage nobody has looked at since launch, and discounts purchased before anyone knew what steady-state looked like. Here's the FinOps audit framework we run for clients, in the order the steps have to happen — because doing them out of order destroys most of the savings.</p><h2>Step 1: Make the bill legible before you cut anything</h2><p>You cannot optimize spend you can't attribute. The first week of every audit is tagging and allocation, not cutting:</p><ul><li>Enforce a minimal tag set — <code>service</code>, <code>environment</code>, <code>team</code>, <code>cost-center</code> — via Terraform defaults and an AWS Config rule (or SCP) that flags untagged resources.</li><li>Turn on Cost and Usage Reports feeding Athena, plus cost allocation tags in Cost Explorer, so every line item maps to an owner.</li><li>Split shared-platform costs (EKS clusters, NAT gateways, observability) with a simple, documented allocation key rather than pretending they don't exist.</li></ul><p>On a typical mid-size AWS estate this stage finds 5–10% of spend that belongs to nothing — orphaned volumes, forgotten load balancers, snapshots from 2023. Deleting those is the easy win, but the durable value is that every subsequent decision has an owner.</p><h2>Step 2: Right-size against measured utilization, not vibes</h2><p>Mis-sizing is where the largest single tranche of savings lives. The method matters: size against percentiles of real usage, not averages, and not peak-of-all-time.</p><ul><li>Pull 30 days of CloudWatch metrics (CPU, memory via the CloudWatch agent, network) and let AWS Compute Optimizer make the first pass.</li><li>Size to p95 utilization with headroom for the workload's real burst pattern. A service that idles at 8% CPU on an <code>m5.2xlarge</code> is not \"safe,\" it's a standing 8x overspend.</li><li>Do the same inside Kubernetes: container requests set at 4x actual usage waste nodes just as effectively as oversized EC2. (We cover this in depth in our Kubernetes resource tuning article.)</li><li>Swap <code>gp2</code> volumes for <code>gp3</code> — same or better performance, roughly 20% cheaper, and the migration is online.</li></ul><p>Performance protection is built in: every resize ships behind the same deploy pipeline as code, with the service's latency SLO watched for a soak period. In 500+ projects' worth of this work, right-sizing done against p95 data has not been the thing that caused an incident. Guesswork sizing has.</p><h2>Step 3: Stop paying for compute that's doing nothing</h2><p>Idle capacity hides in predictable places:</p><ul><li><strong>Non-production schedules.</strong> Dev and staging rarely need nights and weekends. Scheduled scale-down (Instance Scheduler, or a simple EventBridge + Lambda pattern, or scaling EKS node groups to zero) removes ~65% of those environments' hours.</li><li><strong>Bin-packing on Kubernetes.</strong> Karpenter or the cluster autoscaler with consolidation enabled turns accurate pod requests into fewer, fuller nodes.</li><li><strong>Spot for interruption-tolerant work.</strong> CI runners, batch jobs, and stateless workers absorb Spot's interruptions easily for a 60–90% discount. We keep stateful and latency-critical services on on-demand/committed capacity.</li><li><strong>The quiet fixed costs.</strong> NAT gateway data processing, idle RDS replicas, over-provisioned OpenSearch clusters, and inter-AZ traffic each deserve a line-item review.</li></ul><h2>Step 4: Only now buy commitments</h2><p>Savings Plans and Reserved Instances are the largest headline discount — and the step most teams do first, which is backwards. Committing to a mis-sized baseline locks the waste in for one to three years.</p><p>Once steps 1–3 have settled for a full billing cycle, the steady-state floor is visible. We typically cover 70–80% of that floor with Compute Savings Plans (flexibility across instance families and EKS) and reserve databases separately with RDS RIs. The remainder stays on-demand to absorb variance. That mix usually lands a 30%+ discount on the covered baseline without ever constraining an architecture decision.</p><h2>Where the 40% actually comes from</h2><p>Across recent engagements the pattern is consistent: roughly 10% from waste removal and storage lifecycle policies, 15% from right-sizing and bin-packing, 5–10% from scheduling and Spot, and the balance from commitments applied to the corrected baseline. None of these touch the request path, which is why performance doesn't move — the SLO dashboards look identical before and after, and that's the acceptance criterion we sign up to.</p><h2>Keeping it cut</h2><p>The audit is worth little if the bill regrows in two quarters. Three guardrails keep it down: AWS Budgets with per-team alerts, Cost Anomaly Detection routed to the owning team's channel (not a central inbox), and cost review as a standing item in the monthly ops review — the same forum where we review SLOs and incidents, because cost is an operational property of the system, not a finance afterthought.</p><h2>Where to go from here</h2><p>If your bill has grown faster than your traffic, start with attribution and 30 days of utilization data — the rest of the framework falls out of what you find. Our <a href=\"/cloud-infrastructure\">cloud infrastructure</a> and <a href=\"/aws-consulting\">AWS consulting</a> teams run this audit as a fixed-scope engagement, typically paying for itself inside the first quarter. <a href=\"/contact\">Talk to engineering</a> if you want the numbers for your estate.</p>"
  },
  {
    "slug": "incident-response-that-builds-trust",
    "title": "Incident Response That Builds Trust: Runbooks, Roles, and Blameless Postmortems",
    "category": "DevOps",
    "date": "Jun 2026",
    "author": "Ryan Mitchell",
    "readTime": "8 min read",
    "seoTitle": "Incident Response That Builds Trust: Runbooks, Roles, and Blameless Postmortems",
    "seoDescription": "The on-call structure behind our 24/7 support: severity levels, incident roles, tested runbooks, honest comms, and postmortems that change things.",
    "html": "<p>The difference between a five-minute blip and a reputation hit is the process you rehearse. The on-call structure behind our 24/7 support isn't heroics or a wall of dashboards — it's a small set of decisions made *before* anything breaks: who leads, what severity means, which runbook applies, and what we tell customers while we work. Twelve years of operating other people's production systems against a 99.99% SLA has distilled it to the following.</p><h2>Severity levels: decided in advance, declared without debate</h2><p>The worst minutes of an incident are the ones spent arguing about whether it *is* one. We use a small severity matrix, defined by user impact rather than component:</p><ul><li><strong>SEV1</strong> — critical user-facing functionality down or data at risk. Page immediately, incident commander assigned, customer comms within 15 minutes.</li><li><strong>SEV2</strong> — degraded service or a failing subset of users. Page the owning team, comms if customer-visible.</li><li><strong>SEV3</strong> — internal impact or contained fault with redundancy holding. Business-hours handling, tracked.</li></ul><p>Two rules make the matrix work. <strong>Anyone can declare an incident</strong> — a support engineer who suspects a SEV2 opens one; a false alarm costs ten minutes, a suppressed real one costs the SLA. And <strong>when unsure, declare high and downgrade</strong> — the reverse ordering is how five-minute blips become two-hour outages.</p><h2>Roles: three hats, never on one head at the same time</h2><p>The classic incident-command structure survives because it fixes the actual failure mode of major incidents — not lack of skill, but everyone debugging and no one deciding.</p><ul><li><strong>Incident Commander (IC)</strong> owns the incident, not the keyboard. The IC decides between mitigation paths, pulls in people, and — critically — does not debug. The moment the IC starts reading logs, the incident has no commander.</li><li><strong>Operations lead</strong> does the hands-on diagnosis and executes changes, following the runbook until there's an explicit, stated reason to leave it.</li><li><strong>Communications lead</strong> owns the status page, customer updates, and the internal stakeholder channel — so that the people fixing the problem aren't also fielding \"any update?\" every four minutes.</li></ul><p>On a small team, one person may hold multiple hats at 3 a.m. — but the hats stay explicit, and escalation adds people to shed them. Every incident channel opens with a pinned message naming who holds which role; it reads bureaucratic and saves its cost within the first ten minutes of any real SEV1.</p><h2>Runbooks: written calm, executed under stress, tested like code</h2><p>A runbook exists so the 3 a.m. responder borrows the judgment of the person who understood the system at 3 p.m. Ours follow a strict shape: symptoms as the operator sees them, immediate mitigation first (restore service, then diagnose), decision points as explicit if/then branches, exact commands with real resource names, and a hard escalation line — \"if not mitigated in 20 minutes, page X.\"</p><p>The discipline that separates working runbooks from wiki archaeology is testing. Runbooks live in Git next to the services they cover, get updated in the same PR as the change that invalidates them, and get *executed* — not reviewed, executed — in scheduled game days where we inject failures (kill a database failover, exhaust a connection pool, expire a certificate) in a controlled window. Every game day finds at least one command that no longer works or one permission the on-call role silently lost. Better to find it on a Tuesday afternoon.</p><p>Game days double as evidence: rehearsed recovery procedures and tested escalation paths map directly onto the availability and incident-management controls in our SOC 2 Type II and ISO 27001 audits. The rehearsal you run for reliability is the same artifact you show the auditor.</p><h2>Communication: the part customers actually remember</h2><p>Users forgive outages; they don't forgive silence or spin. Our comms rules during customer-visible incidents:</p><ul><li>First status-page update within 15 minutes of declaration, even if it only says what we know and when we'll update next.</li><li>Updates on a stated cadence (every 30 minutes for SEV1) *even when nothing has changed* — \"still investigating, next update 14:30\" beats a two-hour gap every time.</li><li>Plain language, no minimizing. \"Some requests\" is a weasel phrase when it's 40% of requests.</li><li>Never promise an ETA you're guessing at. Promise the next update instead.</li></ul><p>An SLA relationship survives on exactly this. Enterprise customers don't expect zero incidents from anyone; they expect to never learn about one from their own users first.</p><h2>Blameless postmortems: where the trust compounds</h2><p>Every SEV1 and SEV2 gets a postmortem within five working days. Blameless doesn't mean consequence-free — it means the analysis assumes people acted reasonably on the information they had, and asks why the *system* made the failure possible and the detection slow. The moment a postmortem can cost someone standing, it starts producing fiction, and fictional postmortems repeat their incidents.</p><p>The mechanics that keep ours honest: a timeline built from logs and chat records rather than memory; contributing factors instead of a single \"root cause\" (there's never just one); and action items with named owners and deadlines, tracked in the same backlog as feature work. We review open postmortem actions in the monthly ops meeting — an action item without an owner is a wish, and a repeated incident with an unshipped action item from last time is the one failure we treat as unacceptable.</p><h2>Where to go from here</h2><p>If you do only three things: write the severity matrix, name the IC role, and run one game day this quarter. The rest of the machinery grows naturally from what the rehearsal exposes. If you'd rather inherit a rehearsed on-call practice than build one — this structure, staffed around the clock, is what our <a href=\"/managed-devops\">managed DevOps</a> and <a href=\"/managed-hosting\">managed hosting</a> clients run on. <a href=\"/contact\">Talk to engineering</a> about what 24/7 coverage would look like for your platform.</p>"
  },
  {
    "slug": "kubernetes-resource-tuning",
    "title": "Kubernetes Resource Tuning: Requests, Limits, and the Cost of Getting Them Wrong",
    "category": "DevOps",
    "date": "May 2026",
    "author": "Sofia Lindqvist",
    "readTime": "9 min read",
    "seoTitle": "Kubernetes Resource Tuning: Requests, Limits, and the Cost of Getting Them Wrong",
    "seoDescription": "What requests and limits actually control, how to set them from real usage data, and the policies that prevent OOMKills and wasted nodes.",
    "html": "<p>Over-provision and you burn money; under-provision and you trigger OOMKills at the worst moment. Setting requests and limits from real usage data — instead of copy-pasted YAML and superstition — is one of the highest-leverage tuning exercises on any cluster we take over. It's also widely misunderstood, because requests and limits do different jobs than most engineers assume, and CPU and memory behave nothing alike when you get them wrong.</p><h2>What requests and limits actually do</h2><p><strong>Requests</strong> are a scheduling promise. The scheduler places a pod on a node with that much unallocated capacity and reserves it on paper. Requests also set your cost floor: a cluster's size is driven by the *sum of requests*, not by actual usage. If your pods request 4x what they use, you're paying for a cluster 4x bigger than your workload — the utilization graphs can show 15% while the autoscaler insists you need more nodes.</p><p><strong>Limits</strong> are a runtime ceiling, enforced differently per resource:</p><ul><li><strong>CPU is compressible.</strong> Exceed the limit and the kernel throttles you via CFS quota. The pod doesn't die; it gets slower — often invisibly, as latency spikes that nobody connects to a limit set two years ago.</li><li><strong>Memory is not.</strong> Exceed the limit and the kernel OOM-kills the container. There is no \"slower\"; there is only restart.</li></ul><p>That asymmetry drives our default policy, which matches what much of the community (and Kubernetes' own contributors) converged on:</p><ul><li><strong>Memory: limit = request.</strong> Predictable, no overcommit surprises, and the pod lands in a well-defined QoS position. A memory limit above the request is a promise the node may not be able to keep, and the bill for that arrives as an OOMKill during your traffic peak.</li><li><strong>CPU: set requests honestly, and usually set no limit.</strong> Unused node CPU goes to waste otherwise; throttling a latency-sensitive service to protect idle capacity is self-harm. The exceptions we respect: multi-tenant clusters with untrusted or spiky neighbors, and workloads that must exhibit consistent performance for benchmarking or compliance reasons.</li></ul><p>QoS classes fall out of these choices: requests = limits gives you <code>Guaranteed</code>, the last evicted under node pressure. For critical system components and stateful workloads we pay for <code>Guaranteed</code> deliberately.</p><h2>Setting values from real data</h2><p>The method is unglamorous: measure, set to a percentile, re-measure.</p><ol><li><strong>Collect 2–4 weeks of per-container usage</strong> from Prometheus (<code>container_memory_working_set_bytes</code>, CPU usage rates), covering at least one traffic peak and, ideally, one deploy day.</li><li><strong>Set memory request (and limit) at the observed p99 working set plus 15–20% headroom.</strong> Working set, not RSS, is what the OOM decision uses.</li><li><strong>Set CPU request at roughly p95 of usage</strong> for latency-sensitive services — enough that the scheduler co-locates honestly — and lower, nearer p80, for batch workloads that can tolerate contention.</li><li><strong>Let tooling do the arithmetic.</strong> VPA in recommendation mode (not auto mode on latency-critical services), or Goldilocks as a friendlier front-end, turns the metrics into per-workload suggestions. KRR does the same as a CLI against Prometheus. We treat these as draft PRs, not autopilot: an engineer reviews each change against the service's failure modes.</li><li><strong>Recheck quarterly and after major releases.</strong> Resource profiles drift with features; the JVM service that needed 2Gi last year may need 3Gi after the new cache landed — or 1Gi after it moved to a queue.</li></ol><p>Two special cases deserve their own paragraph. <strong>JVM and Node.js runtimes size themselves off the container's view of memory</strong> — set heap flags (<code>-XX:MaxRAMPercentage</code>, <code>--max-old-space-size</code>) coherently with the container limit or you'll OOM with a \"healthy\" heap. And <strong>Java/CPU: a JVM that sees one throttled CPU makes bad GC-thread decisions</strong>; give JIT-heavy services genuine CPU at startup or accept minutes of degraded warmup.</p><h2>The cost of getting it wrong, quantified</h2><p>Both failure directions carry a bill:</p><ul><li><strong>Over-provisioning is a silent 30–60% cluster tax.</strong> On one platform we audited, the cluster ran ~200 pods requesting a combined 340 CPU cores while using 60. Correcting requests and letting Karpenter consolidate cut node spend by 45% — with zero application changes. This is routinely the largest line item in the cloud-spend audits we run.</li><li><strong>Under-provisioning bills you in incidents.</strong> OOMKills cluster at the worst moments by construction: memory pressure peaks when traffic peaks. A restart storm during your busiest hour, retries amplifying load onto surviving pods, and a <code>CrashLoopBackOff</code> graph that looks exactly like an outage — because it is one. If you operate against an uptime SLA, as we do at 99.99%, a mis-sized memory limit is not a tuning detail; it's an availability risk with a blast radius.</li></ul><h2>Guardrails so it stays fixed</h2><p>Point-in-time tuning decays. What keeps it honest on the clusters we operate: <code>LimitRange</code> defaults per namespace so nothing ships without requests; <code>ResourceQuota</code> per team so requests stay negotiated rather than inflated defensively; an OPA/Kyverno policy rejecting workloads with no requests, memory limit ≠ request (exceptions documented), or ratios that scream copy-paste; and OOMKill events plus sustained CPU throttling (<code>container_cpu_cfs_throttled_periods_total</code>) alerting to the owning team — not to a central ops inbox where they go to die.</p><h2>Where to go from here</h2><p>Run KRR or Goldilocks against your cluster this week and just *look* at the gap between requested and used — most teams find their answer to \"why is the cluster so expensive\" in that one report. Then fix the ten worst offenders and add the namespace guardrails so the sprawl doesn't return. Our <a href=\"/kubernetes\">Kubernetes practice</a> does this as part of every cluster takeover, usually alongside a broader <a href=\"/cloud-infrastructure\">cloud cost review</a> — <a href=\"/contact\">talk to engineering</a> if you'd like the measured version of this exercise on your workloads.</p>"
  },
  {
    "slug": "multi-tenant-saas-isolation-boundary",
    "title": "Designing Multi-Tenant SaaS: Where to Draw the Isolation Boundary",
    "category": "Engineering",
    "date": "Nov 2025",
    "author": "Marcus Feldt",
    "readTime": "10 min read",
    "seoTitle": "Designing Multi-Tenant SaaS: Where to Draw the Isolation Boundary",
    "seoDescription": "Pooled, siloed, or bridge — how to choose a tenancy model, with a practical comparison of the Postgres isolation options and their trade-offs.",
    "html": "<p>Pooled, siloed, or bridge — the tenancy model you choose shapes cost, compliance, and blast radius for years. We compare the Postgres isolation options we actually deploy — shared tables with row-level security, schema-per-tenant, and database-per-tenant — and the decision framework we use to place each customer tier on that spectrum. This is one of the few architecture decisions that's genuinely expensive to reverse, so it deserves more than a default.</p><h2>The three models, honestly stated</h2><ul><li><strong>Pooled:</strong> all tenants share tables, every row carries a <code>tenant_id</code>, and application or database policy enforces the boundary. Cheapest to run, easiest to operate, one migration per release. The boundary is logical, so a single missing predicate is a cross-tenant data exposure.</li><li><strong>Siloed:</strong> each tenant gets its own database (or cluster). The boundary is physical. Blast radius, noisy neighbors, and \"where is my data\" questions all collapse — and so does your margin if you do it for thousands of small tenants.</li><li><strong>Bridge:</strong> pooled for the long tail, siloed for the tenants who need it. This is where nearly every successful B2B SaaS ends up, so it's worth designing for on day one even if you launch pooled-only.</li></ul><h2>The Postgres options, compared</h2><h3>Shared tables + row-level security</h3><p>Every tenant-owned table carries <code>tenant_id</code>, and Postgres RLS policies enforce the filter at the database layer:</p><ul><li>The application sets <code>SET LOCAL app.tenant_id = '...'</code> per request/transaction; policies compare against <code>current_setting('app.tenant_id')</code>.</li><li>The application role must not be the table owner and must not have <code>BYPASSRLS</code> — otherwise the policies are decoration.</li><li>Every table gets a composite index led by <code>tenant_id</code>, or the policies quietly turn index scans into sequential scans.</li></ul><p>RLS moves tenant filtering from \"every developer remembers the WHERE clause\" to \"the database refuses to return foreign rows.\" That's a control you can point at in a SOC 2 audit, and it converts an entire bug class from data breach to blank page. The costs: policies complicate query plans (measure, don't assume — we've seen anywhere from negligible to 15% overhead on complex joins), and cross-tenant analytics needs an explicit, separately-audited role.</p><h3>Schema-per-tenant</h3><p>One database, one schema per tenant, identical tables in each. It sounds like a tidy middle ground; at scale it mostly isn't. Migrations multiply by tenant count — 2,000 tenants means 2,000 <code>ALTER TABLE</code>s per release, with partial-failure states to reconcile. <code>pg_dump</code> and connection pooling both degrade as schema count grows, and PgBouncer's <code>search_path</code> handling adds sharp edges. We use it narrowly: tens of tenants, heavyweight per-tenant customization, no ambition to reach hundreds.</p><h3>Database-per-tenant</h3><p>Full physical isolation: per-tenant backups and PITR, per-tenant encryption keys, per-tenant restore without touching neighbors, trivially clean data deletion when a contract ends. On AWS this maps to separate RDS/Aurora databases or clusters, and Aurora Serverless v2 has made small siloed tenants materially cheaper than they were. The cost is operational multiplication — fleets of databases need fleet tooling: migrations orchestrated with per-tenant status tracking, monitoring per database, connection routing in a control plane. Terraform makes tenant provisioning repeatable, but you are now operating a fleet, and you should staff and automate accordingly.</p><h2>What actually decides it</h2><p>Four questions settle the boundary faster than any architecture debate:</p><ol><li><strong>What will enterprise procurement demand?</strong> If your target market includes regulated industries, RFPs will ask for dedicated infrastructure, data residency, and customer-managed keys. That's a siloed tier, priced accordingly — isolation is a sellable feature, not just a cost.</li><li><strong>What's the blast radius you can tolerate?</strong> In a pooled model, one tenant's runaway query or a bad migration touches everyone. Statement timeouts, per-tenant rate limits, and workload isolation help, but physics is physics.</li><li><strong>What compliance boundary are you attesting to?</strong> Pooled-with-RLS is defensible under SOC 2 and ISO 27001 when the policies, roles, and testing are evidenced. Some frameworks and some customers will still require physical separation — find out before you commit, not during the deal.</li><li><strong>What can your team operate?</strong> A database fleet without fleet automation fails worse than a pooled model with disciplined RLS.</li></ol><h2>The pattern we deploy</h2><p>For most B2B SaaS we build: <strong>pooled with RLS as the foundation, a siloed tier from day one on paper, and the seams in place to make it real.</strong> Concretely:</p><ul><li><code>tenant_id</code> on every tenant-owned row and in every JWT, resolved once in middleware — never trusted from request bodies.</li><li>RLS policies plus application-level scoping (defense in depth, and the app layer gives better error messages).</li><li>Automated tests that attempt cross-tenant reads and writes on every table, run in CI on every PR.</li><li>A control plane that maps tenant → connection string, so \"move this tenant to dedicated infrastructure\" is a data migration, not a re-architecture.</li></ul><p>One SaaS platform we operate runs ~3,000 tenants pooled and a single-digit number of enterprise tenants on dedicated Aurora clusters — same codebase, same release train, different <code>DATABASE_URL</code>. That flexibility exists because the seam was designed in before it was needed.</p><h2>Where to go from here</h2><p>If you're pre-launch, start pooled with RLS and build the control-plane seam. If you're live and enterprise deals are stalling on isolation questions, the bridge migration is well-trodden and doesn't require a rewrite. Our <a href=\"/saas-development\">SaaS development</a> team has drawn this boundary across a wide range of tenancy scales — <a href=\"/contact\">talk to engineering</a> and we'll walk through where it belongs for yours.</p>"
  },
  {
    "slug": "observability-that-earns-its-keep",
    "title": "Observability That Earns Its Keep: Metrics, Traces, and Logs Without the Noise",
    "category": "DevOps",
    "date": "Feb 2026",
    "author": "Elena Vasquez",
    "readTime": "9 min read",
    "seoTitle": "Observability That Earns Its Keep: Metrics, Traces, and Logs Without the Noise",
    "seoDescription": "How to wire a signal-first observability stack around SLOs and error budgets — fewer dashboards, fewer alerts, faster answers during incidents.",
    "html": "<p>Dashboards nobody reads and alerts everyone mutes are worse than nothing — they cost money, they train on-call engineers to ignore pages, and they create the illusion of coverage right up until an incident proves otherwise. The fix isn't more telemetry. It's wiring the stack around one question: <strong>is the service doing its job for users right now, and if not, where is it failing?</strong> Here's how we build a signal-first setup around SLOs and error budgets on the platforms we operate.</p><h2>Start with SLOs, not with tools</h2><p>Before choosing between Datadog, Grafana Cloud, or a self-hosted LGTM stack, write down what the service promises. An SLO is a user-facing commitment stated as a target over a window: *99.9% of checkout requests complete successfully in under 400ms, measured over 30 days.*</p><p>Two or three SLOs per service is enough — availability and latency cover most, with a freshness or correctness SLO for pipelines. Everything else in the stack exists to answer whether those numbers hold and why they don't. This is also what makes observability legible to the business: our own 99.99% uptime SLA is, internally, a set of SLOs with error budgets and burn-rate alerting behind it, and the same evidence trail satisfies availability commitments in a SOC 2 audit.</p><p>The error budget is the operational payoff. A 99.9% SLO grants ~43 minutes of failure per month. Spent budget is a signal both directions: burning fast means slow down and stabilize; barely touched means you can afford bolder releases. That converts the reliability-versus-velocity argument from opinion into arithmetic.</p><h2>Alert on symptoms, page on burn rate</h2><p>The single highest-leverage change for most teams: delete threshold alerts on causes (CPU &gt; 80%, disk &gt; 70%, pod restarted) and page only on symptoms — the SLO burning.</p><p>We implement multi-window, multi-burn-rate alerting, the pattern documented in Google's SRE workbook:</p><ul><li><strong>Page</strong> when the burn rate is high over both a short and a long window (e.g., 14x budget burn over 5 minutes *and* over 1 hour) — fast, real, needs a human now.</li><li><strong>Ticket</strong> on slow burn (e.g., 2x over 6 hours) — real but not a 3 a.m. problem.</li><li>Everything that used to be an alert becomes a dashboard panel or a recording rule that provides context *during* an incident instead of noise before one.</li></ul><p>On one platform we took over, this collapsed ~220 alert rules to under 30. Pages dropped by roughly 90%; time-to-acknowledge improved because a page once again meant something.</p><h2>Metrics, traces, logs — each with one job</h2><p>The three signals earn their keep differently, and cost discipline lives in respecting that.</p><ul><li><strong>Metrics</strong> answer *whether* and *how much*. RED (rate, errors, duration) per service, USE (utilization, saturation, errors) per resource, all as SLI recording rules in Prometheus/Mimir. The cost killer is label cardinality — a <code>user_id</code> label on a request counter can 100x your bill. We lint for unbounded labels in CI.</li><li><strong>Traces</strong> answer *where*. Instrument once with OpenTelemetry — vendor-neutral, so the backend stays swappable — and propagate context at every boundary, including queues. Head-sample aggressively (1–10%) but use tail-based sampling to keep 100% of errors and slow requests, which are the only traces anyone reads.</li><li><strong>Logs</strong> answer *why*. Structured JSON only, with <code>trace_id</code> stamped into every line so a trace pivots to its exact logs in one click. Tier retention: hot search for 14–30 days, then object storage for the year-plus horizon that audit trails and forensics require — at S3 prices, not index prices.</li></ul><p>The connective tissue matters more than any single signal: dashboard panel → exemplar trace → correlated logs, without changing tools or re-typing a request ID. That pivot is the difference between a five-minute diagnosis and an hour of grep.</p><h2>Dashboards: fewer, and opinionated</h2><p>Every service gets exactly one primary dashboard, generated from a template: SLO status and remaining budget on top, RED metrics below, saturation and dependencies at the bottom. Deep-dive dashboards exist, but they're linked from the primary, not sprawled across a folder of 80 orphans. If a dashboard hasn't been opened in 90 days, we delete it — the query history settles the argument.</p><h2>Make cost a first-class signal</h2><p>Observability bills fail silently. We track telemetry spend per service alongside the SLOs it supports, and review both in the same monthly ops meeting. Typical findings: debug logs shipped to production indexes, metrics nobody queries, and 100% trace sampling on a service that needed 5%. It's routine to cut the telemetry bill 30–50% while *improving* incident response, because the signal that remains is the signal people use.</p><h2>Where to go from here</h2><p>Pick your most critical user journey, write its two SLOs, and build burn-rate paging for just that service. The pattern proves itself in the first incident, and then it scales by template rather than heroics. If you'd rather start from a working reference implementation, this stack — OpenTelemetry, Prometheus, SLO tooling, tiered storage — is what our <a href=\"/infrastructure-monitoring\">infrastructure monitoring</a> practice deploys and operates. <a href=\"/contact\">Talk to engineering</a> about what signal-first would look like on your platform.</p>"
  },
  {
    "slug": "platform-engineering-paved-road",
    "title": "Platform Engineering: Building the Internal Paved Road Your Teams Will Use",
    "category": "Product",
    "date": "Sep 2025",
    "author": "Hannah Brooks",
    "readTime": "10 min read",
    "seoTitle": "Platform Engineering: Building the Internal Paved Road Your Teams Will Use",
    "seoDescription": "Golden paths, developer portals, and templated CI/CD that teams adopt voluntarily — how to build an internal platform that wins on merit.",
    "html": "<p>A platform team only succeeds when self-service is easier than going around it. Golden paths, developer portals, and templated CI/CD that teams adopt because they're genuinely the fastest route to production — that's the entire game. The moment a platform survives on mandate instead of merit, you've built a ticketing bottleneck with better branding, and product teams will spend real creativity routing around it.</p><p>We've built internal platforms for clients and run one ourselves across hundreds of services. The pattern of what works is consistent enough to write down.</p><h2>The platform is a product, and developers are customers who can leave</h2><p>The founding decision isn't technical. Treat the platform as an internal product with users who have alternatives — because they do: the workaround, the shadow AWS account, the copy-pasted pipeline. That framing forces the practices that make platforms stick:</p><ul><li><strong>A roadmap driven by user research</strong>, meaning you interview product teams about their slowest moments, not about your backlog.</li><li><strong>Adoption as the success metric</strong>, not feature count. A platform capability nobody uses is inventory, not value.</li><li><strong>Support as a first-class function.</strong> If asking the platform team a question takes two days, teams stop asking and start diverging.</li></ul><p>The corollary is scope discipline. The strongest platforms we've built started with the *thinnest viable platform*: one golden path, for the most common service shape, done excellently. Breadth came later, pulled by demand rather than pushed by ambition.</p><h2>Golden paths: opinions with an escape hatch</h2><p>A golden path is the supported, pre-paved route from \"new service\" to \"running in production with observability, security, and deploys handled.\" Ours typically bundle: a service template (for a client's dominant stack — say TypeScript/Node.js or Laravel — with structure, health endpoints, and instrumentation in place), a CI/CD pipeline that comes wired for build, test, scan, and progressive deploy, Terraform modules for the standard infrastructure shapes, and dashboards, alerts, and on-call routing generated from service metadata.</p><p>The test we hold every golden path to: <strong>a new service reaches production in under a day, with security scanning, SLO dashboards, and deploy automation included, and the team wrote only business logic.</strong> When that's true, adoption doesn't need a mandate — the path wins because the alternative is two weeks of yak-shaving.</p><p>Two design rules keep the path golden rather than gilded-cage:</p><ol><li><strong>Opinionated defaults, documented exits.</strong> Teams can leave the path, but leaving is explicit: you take on the ops burden you're declining, and the platform team knows where the exceptions are. Most teams, offered a genuinely good default, don't leave.</li><li>**The path absorbs toil *and* policy.** Baked-in secret scanning, least-privilege IAM, resource limits, and audit logging mean compliance stops being a per-team negotiation. When we take a client through SOC 2 or ISO 27001, the paved road is the control set — evidence comes from the template, once, instead of from forty teams, repeatedly.</li></ol><h2>Templated CI/CD: the highest-leverage piece</h2><p>Of everything in the platform, pipeline templating pays back fastest, because pipeline sprawl is where organizations quietly hemorrhage effort — fifty services, fifty subtly different YAML files, every improvement re-implemented fifty times.</p><p>We build pipelines as versioned, centrally-maintained templates (reusable workflows in GitHub Actions, includes in GitLab CI) that services consume with a few parameters. The service's own config shrinks to ~20 lines declaring what it is; the template supplies build, test, SAST, image signing, deploy strategy, and rollback behavior. When the platform team improves the template — faster caching, a new scanner, a safer canary step — every consuming service inherits it on the next version bump.</p><p>That last property is the strategic one: <strong>templated CI/CD turns platform improvements from campaigns into releases.</strong> It's also a governance instrument — provenance, approvals, and deployment audit trails are enforced in one reviewed place, which is exactly the shape enterprise change-management wants.</p><h2>Developer portals: catalog first, glamour later</h2><p>Backstage (or a lighter equivalent — sometimes a well-kept catalog and a scaffolding CLI is genuinely enough) earns its place in a specific order:</p><ol><li><strong>Software catalog</strong> — every service, its owner, its tier, its docs, its dashboards. The \"who owns this and is it healthy\" question answered in one place is the portal's core value.</li><li><strong>Scaffolding</strong> — \"create new service\" as a form that stamps out the golden-path template with ownership metadata attached from birth.</li><li><strong>Scorecards</strong> — production-readiness visible per service (SLOs defined? runbook current? dependencies patched?), which creates gentle, public pressure toward the paved road without a single mandate.</li></ol><p>The failure mode is inverting the order: six months of portal theming while the catalog is stale. A portal is only as trusted as its data.</p><h2>Measure it like you'd measure any product</h2><p>The metrics that tell you whether the paved road is working: time from repo creation to first production deploy (the single best platform KPI), percentage of services on current template versions, DORA metrics for path users versus off-path teams, and platform NPS taken seriously enough to act on. On one engagement, first-deploy time fell from around three weeks to under a day within two quarters of the golden path landing — and adoption followed the result, not the announcement.</p><h2>Where to go from here</h2><p>Don't start with a portal, a hiring plan, or a platform manifesto. Start with your most-repeated service shape, build one excellent golden path for it, and measure time-to-first-deploy before and after. Everything else — portal, scorecards, broader paths — earns investment from that first win. Our <a href=\"/devops\">DevOps</a> and <a href=\"/managed-devops\">managed DevOps</a> teams build these paved roads inside client organizations, template by template; <a href=\"/contact\">talk to engineering</a> if your teams are still spending their first two weeks on YAML instead of product.</p>"
  },
  {
    "slug": "running-ai-in-production",
    "title": "Running AI in Production: From Prototype to a System You Can Operate",
    "category": "AI",
    "date": "Apr 2026",
    "author": "Aisha Rahman",
    "readTime": "11 min read",
    "seoTitle": "Running AI in Production: From Prototype to a System You Can Operate",
    "seoDescription": "Serving architecture, latency budgets, evaluation gates, and monitoring — what it takes to turn an AI prototype into an operable production system.",
    "html": "<p>A model in a notebook is 20% of the job; the rest is latency budgets, evaluation, and graceful failure. Serving architecture and monitoring — not model choice — are what separate the AI features that quietly work for years from the demos that never survive contact with real traffic. Since standing up our AI &amp; Automation practice in 2024, we've shipped both classic ML and LLM-backed features into production systems, and the operational playbook is more consistent than the hype cycle suggests.</p><h2>Put a gateway between your product and every model</h2><p>The first architectural decision: no application code calls a model provider directly. Everything goes through an internal AI gateway service — ours are typically Node.js/TypeScript — that owns:</p><ul><li><strong>Provider abstraction and fallback.</strong> Primary model, fallback model, and a degraded non-AI path, so a provider incident is a quality reduction rather than an outage.</li><li><strong>Timeouts, retries, and circuit breakers</strong>, tuned per use case instead of inherited from an SDK default of 600 seconds.</li><li><strong>Cost and rate controls.</strong> Per-feature token budgets, per-tenant rate limits, and hard monthly caps. LLM spend is a denial-of-wallet surface; treat it like one.</li><li><strong>The audit trail.</strong> Every request logs prompt version, model version, token counts, latency, and a request ID joined to your tracing. When a customer asks \"why did the system say this?\" — and in regulated environments, they will, and your SOC 2 or internal governance review will too — this log is the answer.</li></ul><p>This single service is also where PII redaction, prompt-injection filtering, and data-residency routing live, which keeps those controls in one audited place instead of scattered across feature teams.</p><h2>Latency budgets decide the architecture</h2><p>Write the budget down before building: *suggestion appears within 800ms p95* is an engineering requirement; \"it should feel fast\" is not. The budget then forces the real decisions:</p><ul><li><strong>Stream anything user-facing.</strong> Time-to-first-token of 300–500ms with streaming feels responsive; five seconds of spinner for the same total latency feels broken.</li><li><strong>Move whatever can be async out of the request path.</strong> Document enrichment, summarization, and classification usually belong on a queue with a webhook or poll, where a 20-second p99 is invisible.</li><li><strong>Cache aggressively.</strong> Exact-match caching for repeated prompts, semantic caching where tolerance allows, and provider-side prompt caching for long static system prompts — routinely a 40–70% cost reduction on read-heavy features.</li><li><strong>Use the smallest model that passes evals.</strong> A well-prompted small model at 4x lower latency and 10x lower cost beats a frontier model on most classification, extraction, and routing tasks. The eval suite, not intuition, makes that call.</li></ul><h2>Evaluation is your regression suite</h2><p>The defining operational difference from conventional software: outputs are non-deterministic and quality regresses silently. A provider updates a model, a teammate \"improves\" a prompt, and a workflow that passed review in March degrades in June without an error in sight.</p><p>The fix is treating evaluation like CI:</p><ul><li><strong>A golden dataset per use case</strong> — a few hundred real (redacted) inputs with expected outputs or scoring rubrics, grown from production failures the way a test suite grows from bug reports.</li><li><strong>Scoring appropriate to the task:</strong> exact/schema match for extraction, assertion checks for structured outputs, LLM-as-judge with spot-checked calibration for open-ended generation.</li><li><strong>Gates in the pipeline.</strong> Prompts and model configs are versioned artifacts in Git; changing either runs the eval suite, and a score drop blocks the merge exactly like a failing test. No prompt edits in a dashboard on Friday afternoon.</li></ul><p>Teams that skip this don't avoid evaluation — they outsource it to their users, one support ticket at a time.</p><h2>Monitor quality, not just uptime</h2><p>Standard observability (latency, error rate, saturation) is necessary and radically insufficient, because an AI feature can be 200-OK-healthy while confidently wrong. The production dashboard needs four layers:</p><ol><li><strong>System:</strong> p50/p95/p99 latency split into time-to-first-token and total, error and timeout rates per provider, fallback activation rate.</li><li><strong>Cost:</strong> tokens and spend per feature per day, with anomaly alerts — a prompt bug that doubles context length is a cost incident you want to catch in hours.</li><li><strong>Quality:</strong> sampled production outputs scored continuously against the same rubrics as offline evals, watching for drift as real inputs diverge from the golden set.</li><li><strong>Product outcome:</strong> acceptance rate of suggestions, edit distance before use, thumbs-down rate, task completion. This is the layer that tells you whether the feature deserves to exist.</li></ol><h2>Design the failure modes on purpose</h2><p>Every AI feature we ship has an answer to \"what happens when the model is down, slow, or wrong\" written before launch: which fallback model, what the non-AI degraded path is, what gets validated before an output touches a downstream system (schema validation on structured outputs is non-negotiable), and which decisions require a human in the loop. Graceful failure is a product decision; retrofitting it during a provider outage is neither graceful nor a decision.</p><h2>Where to go from here</h2><p>If you have a promising prototype, resist wiring it straight into the product. Stand up the gateway, write the latency budget, build a hundred-case eval set — a few weeks of work that determines whether you're operating a system or babysitting a demo. Our <a href=\"/ai-automation\">AI &amp; Automation</a> team builds this production scaffolding as a standard engagement; <a href=\"/contact\">talk to engineering</a> about what your prototype needs before it meets real users.</p>"
  },
  {
    "slug": "shifting-security-left",
    "title": "Shifting Security Left Without Slowing Engineering Down",
    "category": "Security",
    "date": "Oct 2025",
    "author": "Jonas Weber",
    "readTime": "7 min read",
    "seoTitle": "Shifting Security Left Without Slowing Engineering Down",
    "seoDescription": "How to embed SAST, dependency and secret scanning, and policy-as-code at the pull request without wrecking merge times or developer trust.",
    "html": "<p>Security gates bolted on at release time get bypassed under deadline pressure. How we embed SAST, scanning, and policy-as-code at the pull request instead — where feedback is cheap, context is fresh, and nobody has to choose between the release date and the finding — is the subject of this post. The uncomfortable truth first: most \"shift left\" rollouts fail not because the tools are bad, but because they're deployed in a way that teaches engineers to route around them.</p><h2>Why release-time gates always lose</h2><p>A security review two days before launch discovers problems at the moment they're most expensive to fix and most tempting to waive. The finding is real, the deadline is real, and the deadline usually wins — with a risk-acceptance ticket that nobody revisits. Multiply by every release and you've built a process whose main output is exception paperwork.</p><p>The same check at the pull request changes the economics completely. The diff is small, the author has full context, the fix is minutes instead of days, and the vulnerability never enters the main branch. Nothing about the *control* changed — only its position — and position is nearly everything.</p><h2>The PR-stage toolchain, and the tuning that makes it survivable</h2><p>On the stacks we run (Laravel, Node.js/TypeScript, React/Next.js on AWS and Kubernetes), the pull request pipeline carries five checks:</p><ul><li><strong>SAST with a curated ruleset.</strong> We run Semgrep with a deliberately small set of high-confidence rules — injection, broken authz patterns, dangerous deserialization, framework-specific footguns — not the 2,000-rule default pack. A SAST tool's value is inversely proportional to its false-positive rate.</li><li><strong>Secret scanning that blocks.</strong> Gitleaks (plus the platform's native push protection) on every push. This is the one check we run with zero tolerance, because a leaked credential is an incident, not a code-quality issue. Pre-commit hooks catch most of them before they ever reach the remote.</li><li><strong>Dependency scanning tied to reachability.</strong> Alerting on every CVE in the tree buries teams; we prioritize by whether the vulnerable function is actually reachable and whether the service is internet-facing. Auto-merge for patch-level dependency updates keeps the backlog from compounding.</li><li><strong>IaC scanning.</strong> Checkov or tfsec on the Terraform plan — public buckets, permissive security groups, missing encryption — because a misconfigured S3 bucket ships through a PR just like code does.</li><li><strong>Policy-as-code for the rules that are really decisions.</strong> OPA/Conftest checks encode organizational policy: no container runs as root, no service without resource limits, no unencrypted data store. These aren't vulnerability findings; they're the paved road, enforced.</li></ul><h2>The rollout discipline that separates adoption from mutiny</h2><p>The tooling above can be deployed in a week. Whether engineering still tolerates it in month six depends on four rules:</p><ol><li><strong>Baseline the existing debt.</strong> Turning on blocking SAST against a five-year-old codebase generates 3,000 findings and instant resentment. New findings on changed code block; the historical baseline becomes a tracked, prioritized backlog. You're drawing a line, not boiling the ocean.</li><li><strong>Block only on high confidence.</strong> Anything with a meaningful false-positive rate comments on the PR instead of failing it. Every false block spends trust you'll need later.</li><li><strong>Budget the latency.</strong> Security checks get a hard budget inside the PR pipeline — for us, under three minutes added, achieved by scanning diffs rather than whole repos and running checks in parallel. A ten-minute security stage slows every merge for every team, which is precisely the \"slowing engineering down\" this post promises to avoid.</li><li><strong>Every block has an exit.</strong> A documented, logged exception path (label plus required reviewer) prevents the workaround culture that kills these programs. Exceptions are reviewed monthly; a rule that's exception'd constantly is a rule that needs fixing.</li></ol><h2>The compliance dividend</h2><p>Done this way, shift-left doesn't just reduce vulnerabilities — it manufactures audit evidence as a by-product. Every PR carries a record of which checks ran, what they found, who approved exceptions, and when. When our SOC 2 Type II and ISO 27001 audits come around, secure-development and change-management controls are demonstrated from pipeline logs, not reconstructed from interviews. Clients pursuing their own certifications inherit the same posture: the pipeline *is* the control, and the Git history *is* the evidence.</p><p>That's worth stating to whoever funds this work: the same pipeline that catches an SSRF at the PR also shortens your next audit and your next enterprise security questionnaire.</p><h2>What we deliberately leave right</h2><p>Shift left doesn't mean abandon the right side. Runtime controls — WAF, egress restrictions, container runtime monitoring — and periodic penetration tests stay, because some classes of failure (auth logic flaws, business-logic abuse, chained misconfigurations) don't show up in a diff. The PR gate raises the floor; it doesn't remove the need for a ceiling.</p><h2>Where to go from here</h2><p>Start with secret scanning this week — it's the highest severity-to-effort ratio in security tooling — then add diff-scoped SAST with a small blocking ruleset, then IaC checks. Resist enabling everything at once; sequencing is the difference between a security program and a Slack channel full of ignored bot comments. Our <a href=\"/security-compliance\">security &amp; compliance</a> team runs this rollout inside existing pipelines without pausing delivery — <a href=\"/contact\">talk to engineering</a> about where your pull requests are unguarded today.</p>"
  },
  {
    "slug": "terraform-at-scale",
    "title": "Terraform at Scale: Module Design, State Hygiene, and Drift You Can Trust",
    "category": "Cloud",
    "date": "Dec 2025",
    "author": "Tomas Berg",
    "readTime": "8 min read",
    "seoTitle": "Terraform at Scale: Module Design, State Hygiene, and Drift You Can Trust",
    "seoDescription": "An opinionated guide to composable Terraform modules, small state files, environment promotion, and drift detection that stays trustworthy.",
    "html": "<p>IaC falls apart when state balloons and modules become copy-paste sprawl. Our opinionated guide to composable modules and environment promotion comes from operating Terraform across hundreds of client environments — the same repos our engineers plan and apply daily, twelve years into running infrastructure for other people. The failure modes are remarkably consistent, and so are the fixes.</p><h2>Modules: small, composable, versioned</h2><p>The most common anti-pattern we inherit is the god-module: one <code>terraform-aws-platform</code> module with 90 input variables that provisions VPC, EKS, RDS, and IAM in a single apply. It couples unrelated lifecycles (a subnet change now plans against your database), makes review impossible, and turns every upgrade into a big bang.</p><p>The rules we hold modules to:</p><ul><li><strong>One responsibility per module.</strong> A module provisions a VPC, or an EKS cluster, or a Postgres instance with its parameter group, alarms, and backups — not all three. If describing a module needs the word \"and\" twice, split it.</li><li><strong>Composition happens in root modules, not inside child modules.</strong> Child modules take IDs and ARNs as inputs; they don't look up or create their dependencies.</li><li><strong>Version everything.</strong> Modules live in their own repos (or a well-bounded monorepo) with semver git tags, and consumers pin exact versions. <code>ref=main</code> is how one team's refactor becomes another team's outage.</li><li><strong>Fewer knobs, stronger opinions.</strong> Every variable is a promise you maintain forever. Our internal modules encode the decisions we want made once — encryption on, logging on, tags mandatory — and expose only what genuinely varies per consumer.</li></ul><p>A useful smell test: if adding a service requires copy-pasting 400 lines from the last service's directory, you don't have modules, you have templates with extra steps.</p><h2>State: small blast radius, boring storage</h2><p>State design is risk design. Every resource in a state file is exposed to every apply against that file, so the size of your state is the size of your blast radius.</p><ul><li><strong>Split state by environment first, then by lifecycle/domain.</strong> Networking, cluster, data stores, and per-service infrastructure each get their own state. A bad apply to an app's queue should have no physical path to the VPC.</li><li><strong>Connect layers with <code>terraform_remote_state</code> or data sources</strong>, reading outputs — never by importing another layer's resources into your own state.</li><li><strong>Storage is S3 with versioning and native lockfile locking</strong> (or the DynamoDB lock table on older Terraform), encrypted, access-logged, and IAM-restricted to the CI role plus a break-glass group. State files contain secrets whether you like it or not; treat access to them as the privileged operation it is — ours falls under the same access-review controls as production databases in our ISO 27001 scope.</li><li><strong>Watch plan times.</strong> When a plan takes ten minutes, engineers batch changes, reviews get shallow, and the feedback loop that makes IaC safe erodes. Plan time is the practical ceiling on state size.</li></ul><h2>Environment promotion: directories, not workspaces</h2><p>For environment separation we use a directory per environment (<code>envs/dev</code>, <code>envs/staging</code>, <code>envs/prod</code>), each a thin root module that composes the same versioned modules with different variables — not Terraform workspaces, which hide the environment in runtime context, and not copy-pasted stacks, which drift apart within a quarter.</p><p>Promotion is then a version bump you can read in a diff: staging runs <code>vpc v3.2.0</code>, and promoting to prod is a one-line PR changing prod's pin from <code>v3.1.4</code> to <code>v3.2.0</code>, with the plan output attached. The change that reaches production is byte-for-byte the change that soaked in staging. That property — reviewable, ordered, evidenced promotion — is also precisely what a change-management auditor asks for, and with this layout the Git history *is* the audit trail.</p><h2>Drift you can trust</h2><p>Drift — the live estate diverging from code — comes from console hotfixes, scripts, and other tools sharing the account. Undetected, it makes every future plan a surprise. Our approach:</p><ol><li><strong>Scheduled <code>terraform plan</code> on every state, nightly</strong>, with non-empty plans posted to the owning team's channel. Plan output is the drift report; no extra tooling required to start.</li><li><strong>Classify before correcting.</strong> Drift is either an emergency fix that needs back-porting to code, an unauthorized change that needs a conversation, or a resource Terraform shouldn't own. Each has a different fix; auto-reverting all three alike is how you re-break an incident mitigation at 2 a.m.</li><li><strong>Shrink write access.</strong> Humans get read-only in production accounts by default; changes flow through CI. Drift detection matters in inverse proportion to how many hands can create drift.</li><li><strong>Policy as code in the pipeline.</strong> OPA/Conftest (or Sentinel) checks on the plan JSON block public S3 buckets, unencrypted volumes, and missing tags before apply — turning standards from a wiki page into a merge gate.</li></ol><p>The CI shape is deliberately boring: PR triggers fmt, validate, lint, policy checks, and a plan posted to the PR; merge applies that reviewed plan; applies are serialized per state. No local applies against shared environments, ever.</p><h2>Where to go from here</h2><p>If your Terraform hurts, start with state surgery — split the monolith state along lifecycle lines using <code>terraform state mv</code>, carefully and one layer at a time — then version your modules, then turn on nightly drift plans. Each step pays for itself independently. Our <a href=\"/infrastructure-as-code\">infrastructure as code</a> practice does this migration on live estates without downtime windows; <a href=\"/contact\">talk to engineering</a> if yours has reached the point where applies feel like gambling.</p>"
  },
  {
    "slug": "zero-downtime-deploys-kubernetes",
    "title": "Zero-Downtime Deploys on Kubernetes: Rolling, Blue-Green, and Canary in Practice",
    "category": "DevOps",
    "date": "Mar 2026",
    "author": "Daniel Okafor",
    "readTime": "8 min read",
    "seoTitle": "Zero-Downtime Deploys on Kubernetes: Rolling, Blue-Green, and Canary in Practice",
    "seoDescription": "How rolling updates, blue-green, and canary releases actually behave on Kubernetes, and how to pick the right strategy per workload.",
    "html": "<p>Shipping during business hours should be a non-event. If your team schedules releases for Friday night, freezes deploys before demos, or holds its breath while pods cycle, the deployment mechanism — not the code — is the risk. The strategies below are the ones behind the 99.99% uptime SLA we commit to on the platforms we operate, and none of them is exotic. What matters is matching each one to the workload it actually fits.</p><h2>The prerequisites nobody skips twice</h2><p>Every zero-downtime strategy fails without the same three foundations, so we fix these before arguing about rollout styles:</p><ul><li><strong>Graceful shutdown.</strong> The app must handle SIGTERM: stop accepting new work, drain in-flight requests, then exit. We pair this with a <code>preStop</code> sleep of 5–10 seconds so the endpoint controller removes the pod from Service endpoints before the process stops.</li><li><strong>Honest readiness probes.</strong> A readiness probe that returns 200 before the connection pool is warm will route traffic into cold pods. Probe the things the request path depends on — not just \"the process is up.\"</li><li><strong>Backward-compatible releases.</strong> Two versions run simultaneously during any rollout. Schema changes follow expand/contract: add the column in release N, migrate and switch reads in N+1, drop in N+2. This single discipline eliminates most \"we can't roll back\" incidents.</li></ul><p>With those in place, the strategy choice is mostly about blast radius versus cost.</p><h2>Rolling updates: the default, and usually enough</h2><p>Kubernetes' native <code>RollingUpdate</code> replaces pods incrementally. The two knobs that matter are <code>maxSurge</code> (extra pods allowed during rollout) and <code>maxUnavailable</code> (pods you tolerate losing). For latency-sensitive services we run <code>maxSurge: 25%, maxUnavailable: 0</code> — capacity never dips, at the cost of temporarily scheduling extra pods.</p><p>Add a <code>PodDisruptionBudget</code> so node drains and cluster upgrades respect the same availability floor as deploys. Without one, a routine node rotation can take out more replicas than your rollout ever would.</p><p>Rolling updates are the right answer for stateless services where a bad version is caught quickly by probes and the cost of a brief mixed-version window is low. Their weakness: rollback is another rolling update. If the new version is subtly broken — wrong config, degraded latency — you serve some percentage of bad traffic until you notice.</p><h2>Blue-green: instant cutover, instant rollback</h2><p>Blue-green runs the new version (green) at full capacity alongside the old (blue), then flips traffic atomically — in the simplest form, by patching a Service selector; with an ALB or ingress controller, by swapping target groups.</p><p>What you buy:</p><ul><li><strong>One-command rollback.</strong> Flip the selector back. Seconds, not minutes.</li><li><strong>Full pre-production validation against production infrastructure.</strong> Smoke tests hit green over the real database, real caches, real IAM, before a single user does.</li><li><strong>A clean audit story.</strong> For SOC 2-audited platforms, \"traffic switched at 14:02 UTC after automated smoke suite passed\" is a control you can evidence, not a narrative.</li></ul><p>What it costs: double capacity during the window, and it does nothing for you if the failure mode only appears under real traffic. It also sharpens the schema question — both stacks share one database, so expand/contract is mandatory, not advisable.</p><p>We reserve blue-green for payment paths, auth services, and anything where the acceptable bad-traffic budget is effectively zero.</p><h2>Canary: let production data make the call</h2><p>Canary releases send a small slice of real traffic — we typically start at 5% — to the new version and promote only if metrics hold. On our stacks this is Argo Rollouts driving traffic shifts through the ingress or service mesh, with analysis templates that query Prometheus between steps: error rate, p99 latency, and one or two business-level signals (checkout completion, job success rate).</p><p>The step that teams skip, and shouldn't, is automated analysis. A canary you promote by eyeballing Grafana is a rolling update with extra steps. Codify the thresholds: if p99 regresses more than 10% against the stable baseline over a 10-minute window, the rollout aborts and rolls back without a human in the loop.</p><p>Canary's trade-offs are real: you need enough traffic for 5% to be statistically meaningful, session affinity questions for stateful flows, and version-aware observability (every metric labeled by rollout revision). For a low-traffic internal admin service, that machinery is overhead. For the request path that earns the revenue, it's the cheapest insurance available.</p><h2>Picking per workload</h2><p>The pattern we apply across client platforms:</p><ul><li><strong>Stateless internal services, workers, cron jobs</strong> → rolling update with <code>maxUnavailable: 0</code> and a PDB. Done.</li><li><strong>Revenue-critical, high-traffic APIs</strong> → canary with automated metric analysis.</li><li><strong>Auth, payments, anything with a zero-bad-traffic requirement</strong> → blue-green with a smoke suite gating the flip.</li><li><strong>Databases and stateful sets</strong> → none of the above; these get their own maintenance procedures, and the app layer absorbs failover.</li></ul><p>One SaaS platform we operate runs all three simultaneously across ~40 services. The strategy lives next to the service definition in Git, so it's reviewed, versioned, and consistent — not tribal knowledge in a deploy channel.</p><h2>Where to go from here</h2><p>Start by making one service boring to deploy: graceful shutdown, honest probes, <code>maxUnavailable: 0</code>, a PDB, and expand/contract migrations. Then add canary analysis to the service whose failures cost the most. Zero-downtime is not a tool you install; it's a set of defaults you stop deviating from.</p><p>If you'd rather compress that timeline, this is the day job of our <a href=\"/devops\">DevOps practice</a> — or <a href=\"/contact\">talk to engineering</a> about how your current pipeline would need to change.</p>"
  }
];
